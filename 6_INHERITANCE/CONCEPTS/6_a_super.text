Super is used to refer to the parent class. 
It is used to call the parent class’s properties and methods.

The super keyword in Dart is used inside a subclass to:
Access members (methods or properties) of the parent class (superclass).
Call the constructor of the parent class.